// === strict equality operator
// == loose equality ooperator (performs type coercion)
//
console.log(`Number age
`);
const age = 18;
if(age === 18) console.log(`Just an Adult (strict)`);
if(age == 18) console.log(`Just an Adult (loose)`);

console.log(`
String Age
`);
const _age = '18';
if(_age === 18) console.log(`Just an Adult (strict)`);
if(_age == 18) console.log(`Just an Adult (loose)`);

